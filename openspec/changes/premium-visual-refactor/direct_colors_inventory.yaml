# Direct Colors Inventory — P0-UI0 Premium Visual Refactor
# Generated: Mon Jul 27 2026
# Scope: Any color NOT using MichiTheme.colors.* tokens

summary:
  total_direct_hex_colors: 8
  total_string_literals: 2
  severity: low
  assessment: >
    The QML codebase is remarkably clean. Materials, components, shell,
    and theme files have ZERO hardcoded colors. The only exceptions are:
    1) SettingsAppearancePage accent picker (intentional, correct)
    2) Two error banner labels using "white" instead of textOnError

hardcoded_hex:

  - id: DC01
    file: ui_qml/pages/settings/SettingsAppearancePage.qml
    lines: "102-107"
    type: intentional_configuration
    colors:
      - hex: "#8FB7FF"
        name: "Azul"
      - hex: "#A78BFA"
        name: "Morado"
      - hex: "#FF7A00"
        name: "Naranja"
      - hex: "#4ADE80"
        name: "Verde"
      - hex: "#F87171"
        name: "Rojo"
      - hex: "#F0F2F8"
        name: "Blanco"
    risk: null
    notes: >
      These are accent picker model data entries — they define the available
      accent colors for user selection. This is intentional and correct.
      The matching theme tokens are updated dynamically when selected.

  - id: DC02
    file: ui_qml/pages/settings/SettingsAppearancePage.qml
    lines: ["133", "138"]
    type: comparison_literal
    colors:
      - hex: "#8FB7FF"
    risk: null
    notes: >
      Comparison against default accent for selected state detection.
      Should use MichiTheme.colors.accentPrimary comparison if feasible.

string_literals:

  - id: SL01
    file: ui_qml/pages/nowplaying/NowPlayingPage.qml
    line: 95
    value: '"white"'
    context: Error banner text color
    risk: low
    fix: Replace with MichiTheme.colors.textOnError
    notes: >
      Line 95: `color: "white"` in error banner RowLayout.
      Line 103 already correctly uses `MichiTheme.colors.textOnError` for the close button.

  - id: SL02
    file: ui_qml/pages/devices/MobilePairingPage.qml
    line: 85
    value: '"white"'
    context: Error/status text color
    risk: low
    fix: Replace with MichiTheme.colors.textOnError
    notes: >
      Line 85: `color: "white"` in status display.
      Other text on same page uses tokenized colors correctly.

# Verification: Confirmed ZERO hardcoded colors in:
#   ✅ ui_qml/materials/ (7 files)
#   ✅ ui_qml/components/ (all files)
#   ✅ ui_qml/shell/ (AppShell, Sidebar, HeaderBar, PageStack)
#   ✅ ui_qml/theme/ (MichiColors, MichiTheme, etc.)
#   ✅ ui_qml/pages/ (except SettingsAppearancePage)

tokenization_score:
  materials: 100%
  components: 100%
  shell: 100%
  theme: 100%
  pages: 99.8%
  overall: 99.9%
