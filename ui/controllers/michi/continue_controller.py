"""MichiContinueController — manages handoff playback to Micro Server.

Phase 3: UI for "Continue on server" action.
Do not connect to window.py yet.
"""
from __future__ import annotations

import logging

logger = logging.getLogger("michi.controller.continue_on_server")


class MichiContinueController:
    """Manages handing off playback queue to a remote Micro Server."""
    def __init__(self):
        pass
