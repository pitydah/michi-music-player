name: CI

"on":
  push:
    branches: [main, michi-core-convergence, qml-production-parity-x10-v2, qml-real-functions-x10-v3]
  pull_request:
    branches: [main, michi-core-convergence, qml-production-parity-x10-v2, qml-real-functions-x10-v3]
  workflow_dispatch:

jobs:
  qml-validation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: |
          pip install --upgrade pip
          pip install -e ".[dev]"
          pip install pytest-timeout build wheel
      - name: Python compile
        run: python -m compileall -q -x '.venv/|\.tmpl\.' .
      - name: Ruff lint
        run: ruff check . --output-format concise
      - name: QML compile
        run: QT_QPA_PLATFORM=offscreen timeout 120 python scripts/qml_compile_all_v13.py
        continue-on-error: true
      - name: QML productive runtime
        run: QT_QPA_PLATFORM=offscreen timeout 120 python scripts/qml_productive_runtime_v13.py
        continue-on-error: true
      - name: QML tests
        run: QT_QPA_PLATFORM=offscreen timeout 300 python -m pytest tests/qml -q --timeout=300 -m "not isolation" --junitxml=artifacts/qml-v13.xml
      - name: Evidence V13
        run: python scripts/qml_evidence_v13_collect.py --junit artifacts/qml-v13.xml --expected-sha $(git rev-parse HEAD)
      - name: Gate V13
        run: python scripts/qml_productive_gate_v13.py
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: qml-evidence-v13
          path: artifacts/

  wheel-verify:
    runs-on: ubuntu-latest
    needs: [qml-validation]
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - run: |
          pip install --upgrade pip
          pip install -e ".[dev]"
          pip install build wheel
      - run: python -m build --wheel
      - run: python -c "
import zipfile, sys
with zipfile.ZipFile('dist/michi_music_player-0.2.0a1-py3-none-any.whl') as z:
    names = [n for n in z.namelist() if not n.endswith('/')]
forbidden = [n for n in names if 'ui/window.py' in n or 'legacy_widgets' in n]
if forbidden:
    print('FAIL: wheel contains forbidden files:', forbidden)
    sys.exit(1)
print('PASS: wheel clean')
"
